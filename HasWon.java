/*
File: HasWon.java
Author: Micaila Marcelle
Course: CSC 335
Purpose: Implements the HasWon enumerated type, which will be used when checking
the win condition to tell whether the user has won, lost, or neither (still playing)
 */

public enum HasWon {
    WON, LOST, NOT_DONE;
}
